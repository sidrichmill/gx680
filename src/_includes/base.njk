<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="{{ '/css/reset.css' | url }}">
    <link rel="stylesheet" href="{{ '/css/global.css' | url }}">
</head>
<body>
    <article>
    <h1>{{ title }}</h1>
    {% include 'nav.njk' %}
    <p>{{ description }}</p>
        {{ content | safe }}
    </article>
</body>
</html>
