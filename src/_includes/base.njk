<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="{{ '/css/reset.css' | url }}">
    <link rel="stylesheet" href="{{ '/css/global.css' | url }}">
</head>
<body>
    
 {% include 'nav.njk' %}
    <div class="layout">
        {% include 'menu-toc.njk' %}
        <div class="content">
            <h1>{{ title }}</h1>
            <p>{{ description }}</p>
            <br>
                {{ content | safe }}
        </div>
    </div>
</body>
</html>
